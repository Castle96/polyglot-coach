# SQLite Storage

This directory is the default location for SQLite database files used by Polyglot Coach.
Database files ending in `.db` and `.sqlite` are ignored by git.

## Configuration

Configure the database path through application settings or environment-based configuration once runtime code is added.
